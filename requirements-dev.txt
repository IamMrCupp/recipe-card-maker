# Dev dependencies — lint + test toolchain.
# Pulls in the app runtime (which itself pulls in the build-tooling runtime),
# so a dev/CI environment can lint and test everything in one install.
-r requirements-app.txt
ruff==0.15.14
pytest==9.0.3
httpx==0.28.1  # required by fastapi's TestClient
