# Application runtime dependencies for the recipe app backend (_app/).
#
# Layered on top of the build-tooling runtime: the backend reuses the parser
# and PDF builders in _tools/ as library code, so it needs reportlab too.
#   - Just build PDFs/README?      pip install -r requirements.txt
#   - Run the app backend?         pip install -r requirements-app.txt
#   - Develop (lint + test all)?   pip install -r requirements-dev.txt
-r requirements.txt
fastapi==0.136.3
uvicorn[standard]==0.49.0
sqlalchemy==2.0.50
anthropic==0.107.1  # LLM extraction for the smart importers (§3.D.1) — optional at runtime
recipe-scrapers==15.11.0  # structural website import (§3.D.2); LLM is the fallback
python-multipart==0.0.32  # multipart uploads for photo import (§3.D.3)
pillow==12.3.0  # image downsizing for recipe-image export (§3.E.1) — pinned for byte-deterministic re-encode
h2==4.4.1  # HTTP/2 for website import — some sites' WAFs refuse HTTP/1.1 outright (see _app/web_fetch)
